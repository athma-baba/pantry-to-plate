// src/analytics/index.ts
// Placeholder for Firebase Analytics (GA4) integration
// Replace with actual analytics logic

export function logEvent(event: string, params?: Record<string, any>) {
  // TODO: Add analytics().logEvent(event, params)
}

export function setCurrentScreen(screenName: string) {
  // TODO: Add analytics().setCurrentScreen(screenName)
}
